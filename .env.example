# Polling interval in minutes (default: 5)
POLL_INTERVAL_MINUTES=5

# eBay Browse API — free at developer.ebay.com (takes ~24h to verify)
# Leave blank until approved; the scraper will skip gracefully
EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
